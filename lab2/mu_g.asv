function [x, P] = mu_g(x, P, yacc, Ra, g0)
    Q = Qq(x);
    dQ = dQqdq(x);
    H = (Q')*g0;
    dH = dQ;

    Sk = Ra + dH*P*(dH')



    x = x + P*(H')*(H*P*(H')+Ra)^(-1)*(yacc - H*x)
    P = P - P*(H')*(H*P*(H') + Ra)^(-1)*H*P
end